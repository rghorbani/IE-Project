<div class="widget_login"><div class="page-header">

  				<!-- <h2>Sign up</h2> -->
          <h3>
   Adminstrator Login
    <small>into your account</small>
  </h3>

  				<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :class=>'form', :role=>'form') do |f| %>
  				  <% if defined?(devise_error_messages) %>
            <div class="alert alert-notice"><%= devise_error_messages! %></div>
            <% end %>

  				  <div class="control-group">
              <%= f.label :email, :class=>'control-label' %>
    				  <div class="input-group">
    				  	<span class="input-group-addon"><i class="fa fa-envelope"></i></span>
    				  	<%= f.email_field :email, :placeholder=>'Email', :class=>'input-xlarge', :style=>'width:285px; padding:10px;', :autofocus => true %>
              </div>
  				  </div>

  				  <div class="form-group">
              <%= f.label :password, :class=>'control-label' %>
    				  <div class="input-group">
    				  	<span class="input-group-addon"><i class="fa fa-key"></i></span>
    				  	<%= f.password_field :password, :placeholder=>'Password', :class=>'input-xlarge' %>
              </div>
  				  </div>

  				  <div class="form-group">
              <%= f.label :password_confirmation, :class=>'control-label' %>
    				  <div class="input-group">
    				  	<span class="input-group-addon"><i class="fa fa-key"></i></span>
    				  	<%= f.password_field :password_confirmation, :placeholder=>'Confirm Password', :class=>'input-xlarge' %>
              </div>
  				  </div>

  				  <%= f.submit "Sign up", :class=>'large green button radius' %>
            </div></div><hr><p><a href="#">Forgot Password</a><span class="pull-right"><a href="register.htm">Register for account</a></span></p>

          <% end %>
          <br>
          <div>
            <%= render "devise/shared/links" %>
          </div>
  </div>
</div>
