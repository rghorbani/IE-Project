$('#newNews').modal('hide');

$('#newNews').find('input[type="text"],input[type="email"],textarea,select').val('');

$('#news-group').prepend("<%= escape_javascript(render(:partial => 'new_news_content', :object => @news)) %>");

$('.notification').show(500).html("خبر با موفقیت ایجاد گردید.").delay(3000).hide(500);
