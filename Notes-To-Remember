-Raise JS error if login is unsuccessful from root page.

UI Responsiveness (half)

message (done)

show users in message form (done)

poll (there is a gem)

print view of factor

highcharts

shared public places in building

add delayed jobs for better response time (done) -> multiple ways(see other/commands.txt)